[pytest]
# Factory / configurator suite lives under tests/. Overlay packages under
# app/platform_generator/overlays/** ship product-layout tests that import
# app.core.* modules only present after generation — do not collect them here.
testpaths = tests
asyncio_mode = auto
norecursedirs = .* build dist node_modules overlays __pycache__
