# Vendor mirror of Steward estate blocks

Canonical registration remains in Cerebrum-Blocks `block_registry/` + kit
`private_estate_operations`. This mirror documents the Factory-side dual-register
payload when the Blocks remote cannot be updated from this agent (403).

Re-vendor from Blocks after the Blocks PR lands; do not diverge.

## Platform mirrors (Steward estate kit Phase 0+)

Thin dual-registration mirrors for platform Store blocks required by the expanded
Steward blueprint. Used when CI has no Cerebrum-Blocks checkout. Re-vendor from
Blocks after upstream updates; do not invent behavior here.
