{
  "id": "knowledge",
  "name": "knowledge",
  "version": "1.0.0",
  "description": "Factory vendor mirror of Cerebrum-Blocks block 'knowledge' for dual-registration when Blocks checkout is unavailable (CI). Canonical upstream remains Cerebrum-Blocks — do not diverge.",
  "source": "factory-vendor-mirror",
  "upstream": "cerebrum-blocks/block_registry/knowledge",
  "layer": 1,
  "tags": ["platform", "factory-mirror", "steward-kit"],
  "inputs": [
    {
      "name": "action",
      "type": "string",
      "default": "search",
      "options": ["search"],
      "description": "Factory-known keep-path default. Dual-registered app.blocks._knowledge exposes search_knowledge. Registry / factory adapter has no action input (run() only). Harvest prefers this over the STORE_BLOCK_DEFAULT_ACTIONS fallback."
    }
  ]
}
