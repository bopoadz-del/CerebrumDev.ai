{
  "id": "recommendation_template",
  "name": "recommendation_template",
  "version": "1.0.0",
  "description": "Factory vendor mirror of Cerebrum-Blocks block 'recommendation_template' for dual-registration when Blocks checkout is unavailable (CI). Canonical upstream remains Cerebrum-Blocks — do not diverge.",
  "source": "factory-vendor-mirror",
  "upstream": "cerebrum-blocks/block_registry/recommendation_template",
  "layer": 1,
  "tags": ["platform", "factory-mirror", "steward-kit"],
  "inputs": [
    {
      "name": "action",
      "type": "string",
      "default": "apply_template",
      "options": ["apply_template"],
      "description": "Factory-known keep-path default (sess_5782f2264e0e4ff4). Steward property_onboarding hat role is apply_template. Harvest prefers this over the STORE_BLOCK_DEFAULT_ACTIONS fallback."
    }
  ]
}
