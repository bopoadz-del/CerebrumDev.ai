{
  "id": "workflow",
  "name": "workflow",
  "version": "1.0.0",
  "description": "Factory vendor mirror of Cerebrum-Blocks block 'workflow' for dual-registration when Blocks checkout is unavailable (CI). Canonical upstream remains Cerebrum-Blocks — do not diverge.",
  "source": "factory-vendor-mirror",
  "upstream": "cerebrum-blocks/block_registry/workflow",
  "layer": 1,
  "tags": ["platform", "factory-mirror", "steward-kit"],
  "inputs": [
    {
      "name": "action",
      "type": "string",
      "default": "run",
      "options": ["run"],
      "description": "Factory-known keep-path default (sess_07dff0eaf8f64186). Harvest prefers this over STORE_BLOCK_DEFAULT_ACTIONS. Do not declare steps/result here — those close dispatch known-fields and refuse schema-sample domain keys."
    }
  ]
}
