{
  "id": "workflow",
  "name": "workflow",
  "version": "1.0.0",
  "description": "Factory vendor mirror of Cerebrum-Blocks block 'workflow' for dual-registration when Blocks checkout is unavailable (CI). Canonical upstream remains Cerebrum-Blocks — do not diverge.",
  "source": "factory-vendor-mirror",
  "upstream": "cerebrum-blocks/block_registry/workflow",
  "layer": 1,
  "tags": ["platform", "factory-mirror", "steward-kit"],
  "inputs": [
    {
      "name": "action",
      "type": "string",
      "default": "run",
      "options": ["run"],
      "description": "Factory-known keep-path default. Harvest prefers this over STORE_BLOCK_DEFAULT_ACTIONS."
    },
    {
      "name": "steps",
      "type": "array",
      "description": "Workflow children. prepare_block_input constructs this from the schema sample."
    },
    {
      "name": "result",
      "type": "object",
      "description": "Store kit shim / WorkflowBlock reads input['result'] or out['result']. Schema-sample POST has neither — prepare fills this from the first prepared step (sess_07dff0eaf8f64186)."
    }
  ]
}
