{
  "id": "vector_search",
  "name": "vector_search",
  "version": "1.0.0",
  "description": "Factory vendor mirror of Cerebrum-Blocks block 'vector_search' for dual-registration when Blocks checkout is unavailable (CI). Canonical upstream remains Cerebrum-Blocks — do not diverge.",
  "source": "factory-vendor-mirror",
  "upstream": "cerebrum-blocks/block_registry/vector_search",
  "layer": 1,
  "tags": ["platform", "factory-mirror", "steward-kit"],
  "inputs": [
    {
      "name": "action",
      "type": "string",
      "default": "search",
      "options": ["search"],
      "description": "Factory-known keep-path default (sess_8259e197749b4441). Registry block.json has no action input; Store VectorSearchBlock defaults params.operation to search. Harvest prefers this over the STORE_BLOCK_DEFAULT_ACTIONS fallback."
    }
  ]
}
