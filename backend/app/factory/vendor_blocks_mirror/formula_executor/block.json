{
  "id": "formula_executor",
  "name": "formula_executor",
  "version": "1.0.0",
  "description": "Factory vendor mirror of Cerebrum-Blocks block 'formula_executor' for dual-registration when Blocks checkout is unavailable (CI). Canonical upstream remains Cerebrum-Blocks — do not diverge.",
  "source": "factory-vendor-mirror",
  "upstream": "cerebrum-blocks/block_registry/formula_executor",
  "layer": 1,
  "tags": ["platform", "factory-mirror", "steward-kit"],
  "inputs": [
    {
      "name": "action",
      "type": "string",
      "default": "execute",
      "options": ["execute"],
      "description": "Factory-known keep-path default (sess_bb870f4fb29042f2). Harvest prefers this over the STORE_BLOCK_DEFAULT_ACTIONS fallback."
    }
  ]
}
