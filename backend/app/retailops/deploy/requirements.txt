# Lean runtime dependencies for the generated RetailOps container.
# The default embedding backend is deterministic (no ML deps). Set
# RETAILOPS_EMBED_BACKEND=fastembed and add fastembed to use ONNX embeddings.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.27.0

# Database + retrieval
sqlalchemy>=2.0.30
psycopg[binary]>=3.1.18
pgvector>=0.2.5
alembic>=1.13.0

# Orchestration
langgraph>=0.2.0

# Ingestion parsers
pypdf>=5.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
