{
  "name": "cerebrumdev-cloud",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "set -euo pipefail; cd backend; python3 -m venv venv; ./venv/bin/pip install --upgrade pip; ./venv/bin/pip install -r requirements.txt -r requirements-dev.txt; test -f .env || cp .env.example .env; cd ../frontend; npm ci",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && ENV=dev ./venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI configurator + Factory API on :8000 (Vite proxies /v1 here)"
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev -- --host",
      "description": "Vite React UI on :5173"
    }
  ]
}
