# Pending sibling push — Cerebrum-Steward

Factory-generated product tree lives locally at `factory_outputs/Cerebrum-Steward`
(gitignored). This folder holds the **push kit** because the certification agent
could not create `bopoadz-del/Cerebrum-Steward` (GitHub App 403).

| File | Purpose |
|------|---------|
| `GENERATION_PROOF.json` | Factory commit, artifact checklist, verify results |
| `FILE_MANIFEST.txt` | Full relative file list from last generate |
| `push_steward_sibling.sh` | Init git + push to the sibling repo after you create it |

Do **not** hand-edit the product. If something is wrong, upgrade the Factory and re-run generate.
