# Backend API URL (must match the FastAPI backend)
VITE_API_URL=http://localhost:8001

# Backend API key (must match CEREBRUM_DEV_API_KEY in backend/.env).
# Paste from your vault; never commit real keys.
VITE_API_KEY=
