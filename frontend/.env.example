# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:8000
# Optional API key for header-based auth (dev)
VITE_API_KEY=
# Sentry (platform: javascript-react). Loaded lazily; inert when unset.
VITE_SENTRY_DSN=
