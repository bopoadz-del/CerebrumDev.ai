services:
  - type: web
    name: cerebrumdev-construction-sess_deploy_demo
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    plan: starter
    region: oregon
    envVars:
      - key: CEREBRUM_DOMAIN_KITS
        value: construction
      # Secrets: set these via the Render dashboard (Settings > Environment) and
      # never commit real values to version control.
      - key: CEREBRUM_MASTER_KEY
        sync: false
      - key: CEREBRUM_API_KEY_CDEV
        sync: false
      - key: CORS_ORIGINS
        value: *
      - key: CHROMA_PERSIST_DIR
        value: /app/chroma
      - key: OLLAMA_URL
        value: https://cerebrumdev-ollama.theshovel.ai
      - key: OLLAMA_MODEL
        value: qwen3-coder:480b-cloud
      - key: ENV
        value: production
      - key: PORT
        value: 8000
    disk:
      name: cerebrumdev-construction-sess_deploy_demo-data
      mountPath: /app/data
      sizeGB: 1
